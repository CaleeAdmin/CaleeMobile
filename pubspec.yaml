name: calee_mobile
description: "Mobile calendar, tasks, chores, and household planning for Calee families."
publish_to: 'none'

version: 0.0.2+2

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.3.0
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.0
  url_launcher: ^6.3.2

  # 1. 状态管理与路由

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
